const { When, Given } = require ('@wdio/cucumber-framework');
const { pages } = require('../po');

When('I click the "Add Item" button on the "First Item" "Item Card"', function(buttonName, itemName, componentName){
    
});